
(function($) {
    'use strict';

    Game.Constants = {
        teamIds: {
            player: 1,
            computer: 2
        },
        numPlayerAbilities: 5
    };

    Object.freeze(Game.Constants);

}(jQuery));
